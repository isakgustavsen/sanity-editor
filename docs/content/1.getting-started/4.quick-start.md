---
title: Quick start
description: Render SanityEditor with PortableTextBlock[] in a Nuxt application.
navigation:
  icon: i-lucide-zap
seo:
  title: Quick start
  description: Render your first SanityEditor instance with `PortableTextBlock[]` v-model and optional `ClientOnly` wrapping for SSR boundaries.
---

Use the auto-imported **`SanityEditor`** component with a ref typed as **`PortableTextBlock[]`**:

```vue [app.vue]
<script setup lang="ts">
import type { PortableTextBlock } from '@portabletext/types'

const content = ref<PortableTextBlock[]>([])
</script>

<template>
  <SanityEditor v-model="content" />
</template>
```

For SSR, keep the editor in a **client-only** boundary if you prefer not to ship TipTap on the server:

```vue [app.vue]
<script setup lang="ts">
import type { PortableTextBlock } from '@portabletext/types'

const content = ref<PortableTextBlock[]>([])
</script>

<template>
  <ClientOnly>
    <SanityEditor v-model="content" />
  </ClientOnly>
</template>
```

The module auto-imports `SanityEditor`, `useSanityEditor`, conversion helpers, and default schema exports.

Next: [Using the editor](/guide/using-the-editor) →
